<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/content_menu_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="quizme.olgamrost.com.quizme.MenuMain"
    tools:showIn="@layout/activity_menu_main">


    <Button
        android:layout_width="180dp"
        android:layout_height="wrap_content"
        android:text="@string/Databases"
        android:id="@+id/databases"
        android:onClick="onClickDatabases"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true" />

    <Button
        android:layout_width="180dp"
        android:layout_height="wrap_content"
        android:text="@string/Favorits"
        android:id="@+id/Fav"
        android:onClick="onClickShowFavorits"
        android:layout_alignParentTop="true"
        android:layout_toEndOf="@+id/databases" />

    <Button
        android:layout_width="180dp"
        android:layout_height="wrap_content"
        android:text="@string/Accounting"
        android:id="@+id/accounting"
        android:onClick="onClickAccounting"
        android:layout_alignBaseline="@+id/marketing"
        android:layout_alignBottom="@+id/marketing"
        android:layout_alignParentEnd="true"
        android:layout_toEndOf="@+id/marketing" />

    <Button
        android:layout_width="180dp"
        android:layout_height="wrap_content"
        android:text="@string/Marketing"
        android:id="@+id/marketing"
        android:onClick="onMarketing"
        android:layout_below="@+id/databases"
        android:layout_alignParentStart="true" />

</RelativeLayout>
